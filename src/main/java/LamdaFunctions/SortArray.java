package LamdaFunctions;

import java.util.Arrays;
import java.util.Comparator;

public class SortArray {
//    public String[] sortArray(String[] arr){
//        return Arrays.stream(arr).sorted(Comparator.comparing(String::length)
//                .thenComparing(Comparator.comparing(s->s.substring(s.length()-1)).reversed()))
//                .toArray(String[]::new);
//    }
}
